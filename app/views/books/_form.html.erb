<%= form_with(model: book, local: true) do |f| %>
  <% if book.errors.any? %>
    <div style="color: red; border: 1px solid red; padding: 10px; margin-bottom: 15px; background-color: #fde8e8;">
      <strong><%= book.errors.count %>件のエラーが発生しました:</strong>
      <ul>
        <% book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title, "タイトル" %><br>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.label :author, "著者" %><br>
    <%= f.text_field :author %>
  </div>
  <div>
    <%= f.label :price, "価格" %><br>
    <%= f.number_field :price %>
  </div>
  <div>
    <%= f.submit %>
  </div>
<% end %>